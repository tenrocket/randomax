%table.table.striped
	%thead
		%tr
			%th Name
			%th Description
			%th Price
			%th{colspan: "3"}
	%tbody
		- @gifts.each do |gift|
			%tr
				%td= gift.name
				%td= gift.description
				%td= gift.price
				%td.btn.btn-default= link_to 'Show', gift
				%td.btn.btn-warning= link_to 'Edit', edit_gift_path(gift)
				%td.btn.btn-danger= link_to 'Destroy', gift, method: :delete, data: { confirm: 'Are you sure?' }